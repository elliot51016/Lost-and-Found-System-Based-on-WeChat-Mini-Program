<view class="body">
    <Tab tabList="{{tabList}}" bind:getTab="getTab" />
    <view class="lose-list">
      <ViewCard wx:for="{{list}}" data="{{item}}" wx:key="index"
      bindtap="toDetail" data-info="{{item}}"
       />
    </view>
   
</view>